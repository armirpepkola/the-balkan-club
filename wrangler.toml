name = "the-balkan-club"
compatibility_date = "2026-07-06"

# Define the assets directory using the modern Workers format
[assets]
directory = "dist"

# We will add D1 database bindings here in Phase 2.
# For now, this file exists purely to satisfy the local platform proxy.